/*
 * {{ soc_name }}_descriptors.hpp
 * Generated by PeakRDL-pybind11
 *
 * C++ descriptor classes for SystemRDL register map
 */

#ifndef {{ soc_name.upper() }}_DESCRIPTORS_HPP
#define {{ soc_name.upper() }}_DESCRIPTORS_HPP

#include <cstdint>
#include <string>
#include <memory>
#include <functional>
#include <stdexcept>
#include <vector>
#include <sstream>
#include <iomanip>
#include <unordered_map>
#include <utility>
// POSIX mmap support — used by MmapMaster. Windows ships an alternate
// mapping API (CreateFileMapping / MapViewOfFile) that we don't wrap
// today, so MmapMaster is POSIX-only and these headers (plus the class
// itself + its pybind11 binding) are guarded out of the Windows build.
#if !defined(_WIN32)
#include <sys/mman.h>
#include <sys/stat.h>
#include <fcntl.h>
#include <unistd.h>
#include <cstring>
#include <cerrno>
#endif

namespace {{ soc_name }} {

// Forward declarations
class Master;
class RegisterBase;
class FieldBase;

{% include "descriptors/base_classes.hpp.jinja" %}

{% include "descriptors/forward_declarations.hpp.jinja" %}

{% include "descriptors/registers.hpp.jinja" %}

{% include "descriptors/arrays.hpp.jinja" %}

{% include "descriptors/regfiles.hpp.jinja" %}

{# Regfile-array typedefs land here, *after* ``regfiles.hpp.jinja``.
   ``ArrayBase<<rf>_t>`` contains ``std::vector<<rf>_t>`` and needs
   ``<rf>_t`` to be a complete type — forward declarations don't
   provide ``sizeof``. Register-array typedefs (in ``arrays.hpp.jinja``
   above) have the same constraint with respect to register entry
   types, but registers are defined earlier in this file.
#}
{% include "descriptors/regfile_arrays.hpp.jinja" %}

{% include "descriptors/memories.hpp.jinja" %}

{# Mem-array typedefs land here, *after* ``memories.hpp.jinja``.
   ``ArrayBase<<mem>_t>`` carries ``std::vector<<mem>_t>`` and needs
   ``<mem>_t`` to be complete. Emitted *before* ``addrmaps.hpp.jinja``
   so any addrmap that contains an arrayed mem can reference
   ``<mem>_array_t`` as a member type.
#}
{% include "descriptors/mem_arrays.hpp.jinja" %}

{% include "descriptors/addrmaps.hpp.jinja" %}

{# Addrmap-array typedefs land here, *after* ``addrmaps.hpp.jinja``.
   Same complete-type constraint as ``regfile_arrays.hpp.jinja``:
   ``ArrayBase<<am>_t>`` carries ``std::vector<<am>_t>`` and needs
   ``<am>_t`` to be complete. Emitted *before* ``top_node.hpp.jinja``
   so the SoC class can reference ``<am>_array_t`` member types.
#}
{% include "descriptors/addrmap_arrays.hpp.jinja" %}

{% include "descriptors/top_node.hpp.jinja" %}

} // namespace {{ soc_name }}

#endif // {{ soc_name.upper() }}_DESCRIPTORS_HPP
